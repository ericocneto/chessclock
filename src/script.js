'use strict'

/*======== DOM LOGS ========*/
const dom = {
    spaceStartKey: document.addEventListener("keydown", (e)=> {
        if(e.code==="Space") {
            alert(`START BUTTON PRESSIONED!`)
        }
    })
    
}

/*======== HANDLERS ======== */
